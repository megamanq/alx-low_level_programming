#include <stdio.h>
/**
 * main - entry point
 *
 * descripion: this code prints alphabets
 *
 * Return: is always 0 on succeed
 */

int main(void)
{
	char a = 0;

	for (;a<z;a++)
	{
		putchar(a);
		putchar("\n");
	}
	return (0);
}

#include <stdio.h>
/**
 * main - entry point
 *
 * description: print all singl numbr on base 10
 *
 * Return: 0 when succeed
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	putchar('\n');
	return (0);
}

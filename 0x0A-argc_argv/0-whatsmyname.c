#include <stdio.h>

/**
 * main - print the prog name
 * @argc: argument count
 * @argv: array of potr to char arguments
 * Return: 0 on sucess
 */

int main(int argc __attribute__ ((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}

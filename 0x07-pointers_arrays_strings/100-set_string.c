#include "main.h"

/**
 * set_string - set the value of ptr to a char
 * @s: ptr to ptr that will chng
 * @to: ptr to the src of chng
 * Return: no return
 */

void set_string(char **s, char *to)
{
	*s = to;
}
